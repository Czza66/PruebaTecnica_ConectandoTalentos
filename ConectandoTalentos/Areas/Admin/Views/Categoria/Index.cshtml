@model IEnumerable<ConectandoTalentosSolucion.Models.ViewModels.Crud.Categoria.CategoriaVM>
@{
    ViewData["Title"] = "Lista de Categorias";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-8">
            <h2 class="text-primary"> Lista de categorias</h2>
        </div>
        <div class="col-4 text-right">
            <a asp-action="Create" class="btn btn-primary" style="background:#1B75BB ; color:white;border:none"><i class="bi bi-plus-circle-fill"></i>&nbsp; Agregar Categoria</a>
        </div>
    </div>
    <br />
    <br />
    @if (TempData["RespuestaOperacion"] != null)
    {
        <div class="alert alert-success text-center">
            @TempData["RespuestaOperacion"]
        </div>
    }
    <div class="table-responsive">
        <table id="TblContenido" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th> Imagen </th>
                    <th> Nombre </th>
                    <th> Descripción </th>
                    <th> Orden </th>
                    <th> ¿Esta Activo? </th>
                    <th> Acciones </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var ct in Model)
                {
                    <tr>
                        <td>
                            <img src="@ct.imgCategoria"
                                 alt="Imagen categoría"
                                 style="width:60px; height:60px; object-fit:cover; border-radius:6px;" />
                        </td>
                        <td>@ct.nombre</td>
                        <td>@Html.Raw(ct.descripcion)</td>
                        <td>@ct.orden</td>
                        <td>@(ct.activo ? "Si" : "No")</td>
                        <td>
                            <a asp-route-id="@ct.CategoriaIdEncriptado" asp-action="Edit" class="btn btn-primary text-white">
                                <i class="bi bi-pencil-square"></i> Editar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
@section Scripts {
    <script src="~/js/Maestros/DataTableGenerico.js"></script>
}